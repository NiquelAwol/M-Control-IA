services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcontrol_web
    restart: unless-stopped
    ports:
      - "8088:80"
    volumes:
      - .:/var/www/html
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
